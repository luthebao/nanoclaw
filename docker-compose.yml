services:
  gateway:
    build: .
    command: ["gateway"]
    ports:
      - "18790:18790"
    volumes:
      - ~/.nanoclaw:/root/.nanoclaw
      - /var/run/docker.sock:/var/run/docker.sock
    restart: unless-stopped
