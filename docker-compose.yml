services:
  gateway:
    build: .
    command: ["gateway"]
    ports:
      - "18790:18790"
    volumes:
      - ~/.nanobot:/root/.nanobot
      - /var/run/docker.sock:/var/run/docker.sock
    restart: unless-stopped
